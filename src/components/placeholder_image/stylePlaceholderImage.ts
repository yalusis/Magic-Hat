import { StyleSheet } from 'react-native';
import { AppColors } from '../../constants/colors';

export const stylePlaceholderImage = StyleSheet.create({
    placeholderImage: {
      backgroundColor: AppColors.veryLightGray,
      justifyContent: 'center',
      alignItems: 'center',
    },
  });
